import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.roodek.sheetmusicapp',
  appName: 'sheet-music-app',
  webDir: 'dist'
};

export default config;
